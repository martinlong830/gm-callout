{
  "name": "gm-callout",
  "version": "1.0.0",
  "private": true,
  "description": "Red Poke Scheduler — restaurant staffing and coverage calls (Twilio voice)",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "mobile": "npm run start --prefix mobile",
    "mobile:tunnel": "npm run start:tunnel --prefix mobile"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "twilio": "^5.3.6"
  }
}
